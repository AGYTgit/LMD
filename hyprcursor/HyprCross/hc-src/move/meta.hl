resize_algorithm = none

hotspot_x = 0.5
hotspot_y = 0.5

define_size = 0, frame-1.svg

define_override = move
define_override = grab
define_override = grabbing
define_override = closedhand
define_override = fleur
define_override = dnd-move
